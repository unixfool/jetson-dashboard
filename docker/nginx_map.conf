# WebSocket upgrade map — must be at http{} level, outside server{} blocks
# This file is included by nginx at startup alongside conf.d/default.conf
map $http_upgrade $connection_upgrade {
    default   upgrade;
    ""        close;
}